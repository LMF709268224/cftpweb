# 考生系统回归测试任务台账

## 1. 文档用途

本文档是考生系统回归测试任务的统一台账，用于记录：

- 当前有哪些 GitHub Actions 测试任务。
- 每个任务由什么事件驱动。
- 每个任务测试什么、不测试什么。
- 测试使用 Mock 数据还是真实测试环境。
- 新增、修改、删除任务时需要同步维护哪些文件。
- 测试账号、Token、报告和真实数据的安全边界。

GitHub Actions 的实际执行配置以
`.github/workflows/candidate-e2e.yml` 为准。本文档用于解释和维护该配置，
修改工作流时必须同步更新本文档。

当前基线日期：`2026-08-06`。

## 2. 三个任务总览

| 编号 | GitHub Actions Job | 主要测试对象 | 数据来源 | 是否访问真实环境 | 是否允许修改业务数据 |
| --- | --- | --- | --- | --- | --- |
| 任务一 | `Candidate BFF regression` | Go BFF、HTTP 路由、Handler 业务逻辑 | Fake gRPC Client | 否 | 否 |
| 任务二 | `Candidate Playwright regression` | Vue 页面、浏览器交互、Token 和订单体验 | 浏览器 API Mock | 否 | 否 |
| 任务三 | `Candidate live regression` | 真实 Casdoor、真实站点、真实 BFF、微服务和 Stripe Test Mode；包含只读、可恢复写入和完整业务旅程阶段 | 测试环境真实数据 | 是 | 默认不修改；手动开启后允许可恢复写入或明确需要 Admin 清理的专用测试数据 |

三个任务是三层不同的保护，不互相替代：

- 任务一回答“后端接口代码和业务规则有没有被改坏”。
- 任务二回答“前端页面和用户操作流程有没有被改坏”。
- 任务三回答“部署后的真实测试环境是否连得通、登得进、读得出；手动开启
  可写阶段后，真实写接口是否可执行并能恢复或清理；手动开启完整旅程后，
  认证购买、Stripe 测试支付和课程学习是否真正跑通”。

## 3. 驱动方式

统一工作流名称：`Candidate Portal Regression`。

| 驱动事件 | 任务一 | 任务二 | 任务三 |
| --- | --- | --- | --- |
| Push 到 `v2` | 自动执行 | 自动执行 | 跳过 |
| Push 到 `main` | 自动执行 | 自动执行 | 跳过 |
| Pull Request 指向 `v2` | 自动执行 | 自动执行 | 跳过 |
| Pull Request 指向 `main` | 自动执行 | 自动执行 | 跳过 |
| Actions 手动运行，`run_live_readonly=true` | 执行 | 执行 | 执行只读阶段 |
| Actions 手动运行，`run_live_write=true` | 执行 | 执行 | 执行可写阶段 |
| Actions 手动运行，`run_live_journey=true` | 执行 | 执行 | 执行认证购买和课程学习完整旅程 |
| 多个选项为 `true` | 执行 | 执行 | 在同一个 Job 中按只读、可恢复写入、完整旅程的顺序执行 |
| 三个选项都为 `false` | 执行 | 执行 | 整个任务三跳过 |
| 定时任务 | 当前未配置 | 当前未配置 | 当前未配置 |

普通开发流程只需要：

```powershell
git push
```

Push 后，任务一和任务二会自动执行。任务三访问共享测试环境，为避免环境波动
或真实数据变更影响每次提交，目前只允许在 GitHub Actions 页面手动执行。
任务三默认执行只读阶段；可写阶段和完整业务旅程默认关闭，必须主动勾选。

## 4. 任务一：Candidate BFF regression

### 4.1 目标

检查 `candbff` 的编译质量、HTTP 接口合同、鉴权边界和 Handler 业务逻辑。

### 4.2 执行步骤

GitHub Actions 依次执行：

```powershell
go mod download
go vet ./...
go test ./...
go test -count=1 -coverprofile coverage.tmp ./handler ./server
```

当前覆盖率硬门槛为 `35%`。截至 `2026-08-06`，本地最近一次
Handler/Router 合并覆盖率为 `40.5%`。覆盖率会随代码变化，硬门槛以工作流
配置为准。

### 4.3 测试范围

- 健康检查和 JSON 404。
- 98 个 HTTP Method + Route 组合的精确合同。
- 78 个受保护路由的登录鉴权边界。
- CORS 允许和拒绝规则。
- 登录、回调、Cookie、Token 刷新和退出。
- 用户资料输入校验与格式标准化。
- 会员计划、会员记录、账单和取消规则。
- 商城、Pipeline、学习进度和课程报名。
- 资源包、文件归属、预览和查看地址。
- 测验答题、草稿、提交和完成。
- 考试历史、重考、回调和参数校验。
- 资格申请、证书和文件映射。
- 订单筛选、汇总、详情归属和取消状态。
- 发票归属、完成状态、Stripe URL 和 PDF。
- 消息列表、模板渲染、未读、已读和删除。
- 首页统计数据聚合。
- 公共配置不得泄露 Stripe Secret Key。
- Bundle 订单创建时的考生归属、支付模式、免考 JSON 和订单 ID 转发。
- 考试报名资料的规范化、路由实例 ID 优先级和来源系统标记。
- 课时完成写操作的考生归属、响应透传和微服务异常映射。

### 4.4 测试目录

- `candbff/handler/*_test.go`
- `candbff/server/router_test.go`

Handler 测试按业务模块拆分，例如：

- `payment_test.go`
- `invoice_test.go`
- `membership_test.go`
- `message_test.go`
- `certificate_test.go`
- `resource_pack_test.go`
- `quiz_test.go`
- `enrollment_test.go`
- `write_handlers_test.go`

测试文件统一放在 `candbff/handler` 和 `candbff/server`，不得散落到其他业务
目录。

### 4.5 使用的数据

Handler 测试使用 Fake gRPC Client，不连接真实 Casdoor、Stripe 或微服务，
不会创建真实订单或修改真实账号。

### 4.6 不覆盖的内容

- 真实 Casdoor 是否在线。
- 测试环境 BFF 是否已经正确部署。
- 微服务、Redis、数据库、NATS 是否真实可用。
- Stripe 和第三方考试供应商是否在线。
- 浏览器页面是否能正常显示。

## 5. 任务二：Candidate Playwright regression

### 5.1 目标

在 Chromium 中自动操作 Vue 考生页面，验证前端状态、按钮、跳转和异常处理。

### 5.2 执行步骤

GitHub Actions 依次执行：

```powershell
npm ci
npm run build
npm run test:e2e:typecheck
npx playwright install --with-deps chromium
npm run test:e2e
```

### 5.3 当前测试范围

当前固定执行 25 条测试：

- 未登录访问受保护页面时保留目标地址并进入登录流程。
- Access Token 过期时只刷新一次，并继续停留在当前页面。
- Refresh Token 也失效时清理本地会话并提示重新登录。
- 订单状态和可执行按钮保持一致。
- 托管支付返回后清理查询参数并同步订单状态。
- 嵌入式支付完成后留在订单页面并刷新订单状态。
- 取消未支付订单后校验请求归属并刷新列表。
- 消息批量标记已读、删除及未读状态刷新。
- 会员取消自动续费并刷新为已取消状态。
- 测验选择答案后同步草稿、提交答卷并展示结果。
- 待报名考试从列表进入带正确参数的报名页面。
- 从商城选择认证，填写报名资料，创建订单，完成模拟 Stripe 支付，并在我的认证中看到已购认证。
- 从我的认证进入课程，完成课件，进入测验，选择答案、提交并展示通过结果。
- 首页空数据状态和 JavaScript 崩溃检查。
- 商城空数据状态和 JavaScript 崩溃检查。
- 我的认证空数据状态和 JavaScript 崩溃检查。
- 考试空数据状态和 JavaScript 崩溃检查。
- 档案空数据状态和 JavaScript 崩溃检查。
- 资源包空数据状态和 JavaScript 崩溃检查。
- 资格申请空数据状态和 JavaScript 崩溃检查。
- 证书空数据状态和 JavaScript 崩溃检查。
- 会员空数据状态和 JavaScript 崩溃检查。
- 订单空数据状态和 JavaScript 崩溃检查。
- 消息空数据状态和 JavaScript 崩溃检查。
- 设置空数据状态和 JavaScript 崩溃检查。

### 5.4 测试目录

- `candweb/vue-web/e2e/auth.spec.ts`
- `candweb/vue-web/e2e/business-flows.spec.ts`
- `candweb/vue-web/e2e/orders.spec.ts`
- `candweb/vue-web/e2e/portal-smoke.spec.ts`
- `candweb/vue-web/e2e/support/candidate.ts`
- `candweb/vue-web/playwright.config.ts`

### 5.5 使用的数据

测试会在浏览器层拦截 `/api/**` 请求，返回确定的 Mock 数据。Casdoor、Stripe
和后端接口也会被模拟，因此不需要真实账号，不创建真实订单。

### 5.6 失败报告

任务失败时上传：

- `candweb/vue-web/playwright-report/`
- `candweb/vue-web/test-results/`

报告保留 7 天。

### 5.7 不覆盖的内容

- 真实账号密码是否能登录。
- 真实 Cookie、Token 和刷新 Cookie 是否正确下发。
- BFF 和微服务是否已经部署。
- 真实数据库中的订单、证书和消息是否可读取。
- Casdoor、Stripe 或第三方服务的真实可用性。

## 6. 任务三：Candidate live regression

### 6.1 目标

使用真实测试账号登录真实 Casdoor，并访问部署后的考生测试环境。任务三在
同一个 GitHub Actions Job 中包含默认只读阶段、可选可恢复写入阶段和可选完整
业务旅程阶段。完整旅程不会新增第四个 GitHub Actions Job。

### 6.2 驱动方式

任务三只在 GitHub Actions 手动运行时执行：

1. 打开仓库的 `Actions`。
2. 选择 `Candidate Portal Regression`。
3. 点击 `Run workflow`。
4. 选择要测试的分支。
5. 保持 `run_live_readonly` 为勾选状态。
6. 如需执行可恢复的真实写操作，额外勾选 `run_live_write`。
7. 如需执行认证购买和课程学习完整旅程，额外勾选 `run_live_journey`。
8. 点击绿色的 `Run workflow`。

手动运行时，任务一和任务二也会同时执行。
`run_live_readonly` 默认值为 `true`，`run_live_write` 和
`run_live_journey` 默认值均为 `false`。

#### 6.2.1 Run workflow 三个勾选项

GitHub Actions 的 `Run workflow` 面板中有三个可选项，它们都属于任务三，
但对应不同的测试范围和数据风险：

| GitHub 页面显示名称 | 工作流参数 | 做什么 | 是否修改测试环境数据 | 什么时候勾选 |
| --- | --- | --- | --- | --- |
| `Run the candidate test-environment read-only regression` | `run_live_readonly` | 使用真实考生账号登录 Casdoor，检查 `/health`、`/api/user/me` 和考生端 12 个主要页面，确认真实 BFF、微服务和页面读取链路正常 | 否，只读取真实数据 | 日常部署后巡检、上线前基础回归。默认勾选，通常保持勾选 |
| `Run controlled candidate test-environment writable regression` | `run_live_write` | 临时修改并恢复考生 `bio`；配置了 `E2E_LIVE_WRITE_BUNDLE_ID` 时，还会创建并取消一个未支付订单 | 是，但测试会在 `finally` 中自动恢复或取消 | 修改资料保存、订单创建、订单取消等写接口后使用。运行期间不要人工操作同一账号 |
| `Run paid certification and learning journeys against Stripe Test Mode` | `run_live_journey` | 复用同一个真实考生账号，自动选择一个可购买且包含课程的认证，使用 Stripe 测试卡支付，再进入刚购买的认证完成课件并在可用时提交测验 | 是，会留下已支付订单、认证归属、学习进度和测验结果，不能自动清理 | 验证认证购买、Stripe 回调、Pipeline 创建和课程学习完整链路时使用。运行前必须确认 Stripe Test Mode，并安排 Admin 清理 |

三个勾选项只控制任务三内部执行哪些阶段。无论如何选择，只要手动启动
`Candidate Portal Regression`，任务一和任务二仍会正常执行。

常用选择方式：

| 使用场景 | `read-only` | `writable` | `paid journey` | 说明 |
| --- | --- | --- | --- | --- |
| 普通真实环境巡检 | 勾选 | 不勾选 | 不勾选 | 推荐的日常选择，不修改业务数据 |
| 检查真实写接口 | 建议勾选 | 勾选 | 不勾选 | 先确认读取链路正常，再执行可自动恢复的写测试 |
| 检查购买和学习完整流程 | 建议勾选 | 按需 | 勾选 | 会产生支付和学习数据，运行前后需要 Admin 配合清理 |
| 只运行任务一、任务二 | 不勾选 | 不勾选 | 不勾选 | 任务三整个跳过 |

如果同时勾选多个选项，任务三按照以下顺序串行执行：

1. 只读回归。
2. 可恢复写入回归。
3. 认证购买和课程学习完整旅程。

### 6.3 必需 Secrets

在仓库 `Settings > Secrets and variables > Actions` 中配置：

- `E2E_CANDIDATE_BASE_URL`
- `E2E_CANDIDATE_USERNAME`
- `E2E_CANDIDATE_PASSWORD`

账号密码只能通过 GitHub Secrets 或本地环境变量注入，禁止写入：

- Git 仓库。
- Workflow YAML。
- 测试源码。
- Markdown 文档。
- 测试报告。
- 截图、视频和 Trace。

### 6.4 执行步骤

GitHub Actions 依次执行：

```powershell
npm ci
npm run test:e2e:typecheck
npx playwright install --with-deps chromium
npm run test:e2e:live          # run_live_readonly=true 时执行
npm run test:e2e:live:write    # run_live_write=true 时执行
npm run test:e2e:live:journey  # run_live_journey=true 时执行
```

依赖、类型检查和 Chromium 安装在任务三中只执行一次。前置阶段失败时不会继续
执行后续数据写入阶段，避免在基础环境异常时修改测试数据。

### 6.5 只读阶段当前测试范围

当前共识别 3 条 Playwright 测试，其中主页面测试包含 12 个独立步骤：

- 1 条真实 Casdoor 登录准备测试。
- 1 条 `/health` 和 `/api/user/me` 真实接口检查。
- 1 条主页面真实数据读取测试，依次执行 12 个页面步骤并共享浏览器缓存。

检查的 12 个页面：

- 首页 `/dashboard`
- 商城 `/certifications`
- 我的认证 `/my-certifications`
- 考试 `/exams`
- 档案 `/records`
- 资源包 `/resource-packs`
- 资格申请 `/credentials`
- 证书 `/certificates`
- 会员 `/membership`
- 订单 `/orders`
- 消息 `/messages`
- 设置 `/settings`

每个页面会检查：

- 登录后没有跳回登录页。
- 页面标题正常显示。
- 页面发起的每类主体业务 API 在 45 秒内至少有一条完成；持续遥测、导航栏计数和同路径重复刷新不阻塞页面检查。
- 没有意外的 API 4xx 或 5xx。
- 没有 API 请求失败。
- 没有 JavaScript 页面异常。
- 没有业务代码写入 `console.error`。

资源缩略图接口返回 404 属于可选资源缺失，不单独判定整个页面失败。

### 6.6 只读保护

真实只读测试允许：

- `GET`
- `HEAD`
- `OPTIONS`
- `POST /api/auth/refresh`
- `POST /api/telemetry`
- `POST /api/public/telemetry`

其他 `/api/**` 非只读请求会被浏览器测试直接阻止，并将测试判定为失败。

因此只读阶段不会：

- 创建或取消订单。
- 修改个人资料。
- 修改密码或邮箱。
- 标记或删除消息。
- 提交考试、测验或资格申请。
- 发起真实支付。

### 6.7 测试目录

- `candweb/vue-web/e2e-live/auth.setup.ts`
- `candweb/vue-web/e2e-live/read-only.spec.ts`
- `candweb/vue-web/e2e-live/write.spec.ts`
- `candweb/vue-web/e2e-live/journey.spec.ts`
- `candweb/vue-web/e2e-live/support/live.ts`
- `candweb/vue-web/playwright.live.config.ts`

### 6.8 失败报告和安全

任务失败时只上传 `candidate-live-regression-report`，内容来自：

- `candweb/vue-web/playwright-report-live/`

报告保留 3 天。

真实测试关闭：

- Playwright Trace。
- 自动截图。
- 浏览器视频。

这是为了避免 Cookie、Token、账号信息或真实测试数据进入测试产物。

### 6.9 当前状态

任务三代码和 GitHub Actions Job 已实现，三个 GitHub Secrets 已完成配置。
`2026-08-06` 已在 GitHub Actions 使用真实测试环境完成 3 条测试和 12 个页面
步骤并全部通过。

### 6.10 可写阶段

#### 6.10.1 目标

使用测试环境专用账号验证真实写接口，但只允许具备自动恢复或自动清理能力的
操作。任务三的可写阶段不会在 Push 或 Pull Request 时自动执行，避免并发
写入共享测试数据。

#### 6.10.2 驱动方式

任务三的可写阶段只在 GitHub Actions 手动运行并明确勾选时执行：

1. 打开仓库的 `Actions`。
2. 选择 `Candidate Portal Regression`。
3. 点击 `Run workflow`。
4. 选择要测试的分支。
5. 勾选 `run_live_write`。
6. 按需决定是否同时勾选 `run_live_readonly`。
7. 点击绿色的 `Run workflow`。

`run_live_write` 默认值为 `false`。工作流启动测试时还会注入
`E2E_LIVE_WRITE_ENABLED=true`，测试源码会同时检查该开关，防止本地或其他
Job 误执行。

启用可写阶段的任务三运行使用独立并发组且禁止 `cancel-in-progress`。普通
Push 不会中断正在执行的可写阶段；同一分支重复启动可写阶段时会排队串行
执行，避免清理逻辑因自动取消而来不及运行。

#### 6.10.3 Secrets 和 Variables

可写阶段复用任务三的三个必需 Repository Secrets：

- `E2E_CANDIDATE_BASE_URL`
- `E2E_CANDIDATE_USERNAME`
- `E2E_CANDIDATE_PASSWORD`

可选 Repository Variable：

- `E2E_LIVE_WRITE_BUNDLE_ID`

`E2E_LIVE_WRITE_BUNDLE_ID` 必须指向测试环境中允许当前测试考生购买、价格大于
零、创建后处于可取消状态的专用 Bundle。未配置时，订单创建取消测试会显示
为跳过，不影响资料更新恢复测试。

#### 6.10.4 执行步骤

任务三完成共用的依赖安装、类型检查和 Chromium 安装后执行：

```powershell
npm run test:e2e:live:write
```

#### 6.10.5 当前测试范围

当前包含 1 条必跑写测试和 1 条可选写测试：

- 必跑：读取当前考生资料，临时修改 `bio` 为唯一标记，确认真实读取结果已更新，
  然后在 `finally` 中恢复原值并再次确认。
- 可选：创建专用 Bundle 的未支付订单，确认获得真实订单 ID，并在 `finally`
  中调用 `/api/orders/cancel` 清理该订单。

订单测试明确拒绝 `COMPLETED`、`PAID` 或 `SUCCESS` 状态。专用商品如果会创建
免费或已完成订单，不得配置到该 Variable。

#### 6.10.6 清理和失败恢复

- 资料测试无论断言成功还是失败都会尝试恢复原始 `bio`。
- 订单测试只要取得 `bundle_order_ulid`，无论后续断言成功还是失败都会尝试
  取消订单。
- 如果清理接口本身失败，Job 必须保持失败，不能把残留数据伪装成成功。
- 工作流不会因为同分支的新 Push 自动取消正在运行的可写回归。
- 测试不执行密码、邮箱、真实支付、考试报名、资格申请或学习进度写入。
- 执行任务三的可写阶段前，应避免同一测试账号被人工同时操作。

#### 6.10.7 测试目录和报告

- `candweb/vue-web/e2e-live/write.spec.ts`
- `candweb/vue-web/e2e-live/auth.setup.ts`
- `candweb/vue-web/e2e-live/support/live.ts`
- `candweb/vue-web/playwright.live.config.ts`

失败时上传 `candidate-live-regression-report`，只包含关闭 Trace、截图和视频后的
Playwright HTML 报告，保留 3 天。

### 6.11 完整业务旅程阶段

#### 6.11.1 测试范围

`run_live_journey=true` 时，在任务三内部额外执行一条连续的真实环境旅程，
其中包含购买和学习两个阶段：

- 认证购买：逐个读取商城认证详情，自动选择当前账号可购买、支付金额大于 0
  且包含课程的认证；取消默认课程豁免，检查考生资料完整，创建订单，打开
  Stripe Embedded Checkout，使用 Stripe Test Mode 测试卡支付，并验证进入
  支付成功页。
- 课程学习：等待刚购买的 Pipeline 创建完成，从我的认证进入该认证和第一个
  可学习课程，完成一个未完成课件；如果该认证当前有可用测验，则为每道题
  选择一个答案、提交并验证结果页正常显示。

#### 6.11.2 Stripe 配置

GitHub Actions 不需要保存 Stripe Dashboard 账号、密码、Secret Key 或测试卡号。
必须由测试环境的支付服务提前配置 Stripe Test Mode Key。浏览器测试使用 Stripe
公开测试卡 `4242 4242 4242 4242`、任意未来日期和任意 CVC；这些不是 Secret。

如果测试环境误用了 Stripe Live Mode，禁止运行完整旅程。

#### 6.11.3 账号和可选 Variable

完整旅程复用任务三原有的三个 Secrets：

- `E2E_CANDIDATE_BASE_URL`
- `E2E_CANDIDATE_USERNAME`
- `E2E_CANDIDATE_PASSWORD`

不需要增加另一个考生账号，也不需要配置 Bundle、Pipeline、Course、Lesson 或
Quiz ID。测试会逐个检查商城认证详情，选择第一个 `can_purchase=true`、支付
金额大于 0、关联 Pipeline 且包含课程的认证，支付成功后继续测试刚创建的认证。

可选 Repository Variable：

- `E2E_STRIPE_TEST_EMAIL`

如果不配置，测试使用固定的非真实测试邮箱。

#### 6.11.4 数据清理

完整旅程会产生已支付测试订单、认证归属、课件进度和测验结果。考生不能取消
已支付订单，当前也没有经过确认的自动回滚合同，因此测试不会猜测调用 Admin
取消接口或篡改业务字段。

每次重新执行前必须由 Admin 或测试数据维护人员：

1. 清理或取消上次已支付测试订单。
2. 恢复该考生至少一个认证的 `can_purchase=true` 状态。
3. 重置刚购买认证的课件进度和测验结果。
4. 确认测试环境仍使用 Stripe Test Mode。

完整旅程启用独立串行并发保护，不会被同分支的新 Push 自动取消。

## 7. 任务和代码改动的对应关系

| 改动类型 | 必须检查或修改的任务 |
| --- | --- |
| 新增、删除、重命名 BFF 路由 | 任务一，更新 `candidateRouteContract` |
| 修改 Handler 参数或响应 | 任务一，增加成功、失败、归属和状态测试 |
| 修复后端业务 Bug | 任务一，增加能复现旧 Bug 的回归测试 |
| 修改登录、Token、订单按钮或支付返回体验 | 任务二 |
| 新增考生主页面 | 任务二和任务三 |
| 修改页面依赖的真实只读接口 | 任务一和任务三 |
| 修改 Casdoor 登录或 Callback | 任务一、任务二和任务三 |
| 修改用户资料写接口 | 任务一、任务二和任务三的可写阶段 |
| 修改 Bundle 创建或订单取消接口 | 任务一、任务二和任务三的可写阶段 |
| 修改认证购买、Stripe 支付或课程学习流程 | 任务二和任务三的完整业务旅程阶段 |
| 新增真实数据写操作回归 | 不得加入任务三的只读阶段；仅可加入任务三的可写阶段并提供恢复或清理机制 |
| 修改 GitHub Actions 驱动条件 | 更新 Workflow 和本文档的驱动矩阵 |

## 8. 新增任务时的维护规则

新增第四个或后续任务时，必须完成：

1. 明确任务名称和唯一 Job ID。
2. 明确是 Push、Pull Request、手动还是定时驱动。
3. 明确是否访问真实环境。
4. 明确是否会创建或修改业务数据。
5. 明确测试账号和最小权限。
6. 明确测试数据创建与清理方案。
7. 明确失败报告保存哪些文件、保留多少天。
8. 明确哪些 Secrets 或 Variables 是必需的。
9. 将测试代码放入统一目录，不散落到生产代码目录。
10. 更新本文档的任务总览、驱动矩阵、测试范围和变更记录。

如果任务会产生真实订单、支付、考试报名或资格申请，必须使用
`workflow_dispatch` 手动触发，除非已经具备可靠的自动清理机制。

## 9. 修改任务时的维护规则

修改现有任务时，必须同步检查：

- Workflow 中的 Job 名称、`if` 条件和超时时间。
- `package.json` 或 Go 命令。
- Playwright Config、测试目录和报告目录。
- 测试数量和页面清单。
- GitHub Secrets 名称。
- 覆盖率门槛。
- 文档里的驱动矩阵和测试范围。

修改后至少运行对应的本地检查。

任务一相关修改：

```powershell
cd candbff
go vet ./...
go test -count=1 ./...
```

任务二相关修改：

```powershell
cd candweb/vue-web
npm run build
npm run test:e2e:typecheck
npm run test:e2e
```

任务三相关修改：

```powershell
cd candweb/vue-web
npm run test:e2e:typecheck
npm run test:e2e:live -- --list
npm run test:e2e:live:write -- --list
npm run test:e2e:live:journey -- --list
```

只有在本地安全配置了环境变量，并确认目标是测试环境时，才运行完整的
`npm run test:e2e:live`。只有在明确允许修改测试数据、配置了测试账号，并
确认恢复或清理机制有效时，才运行完整的 `npm run test:e2e:live:write`。只有在
确认 Stripe Test Mode、账号至少有一个可购买认证、认证中存在未完成课件，
并且 Admin 清理安排已就绪时，才运行 `npm run test:e2e:live:journey`。

## 10. 删除任务时的维护规则

删除任务时不得只删除 GitHub Actions Job，还必须检查：

1. 删除对应 Workflow Job。
2. 删除不再使用的测试目录和配置文件。
3. 删除不再使用的 `package.json` Script。
4. 删除不再使用的报告目录配置。
5. 确认相关 Secrets 是否可以从 GitHub 删除。
6. 更新本文档的任务总览和驱动矩阵。
7. 更新其他引用该任务的 README 和 Pull Request 模板。
8. 确认删除后仍有其他测试层保护原来的关键风险。

## 11. 当前未覆盖的后续任务

以下内容仍未进入自动化回归，或只完成了受控子集：

- 真实环境订单创建取消仅在配置专用 Bundle 后执行。
- 真实考试报名、改期、取消和重考。
- 真实资格申请、文件上传和重新提交。
- 已支付测试订单、认证归属、课程进度和测验结果的自动 Admin 清理或重置。
- 页面加载性能指标和大 Chunk 性能门槛。

这些操作会修改真实测试数据。加入任务三的可写阶段前必须先确定测试账号、
最小权限、专用测试数据、清理方式和失败恢复方案；无法自动恢复的操作继续
保留人工测试。

## 12. 变更记录

| 日期 | Commit | 变更 |
| --- | --- | --- |
| 2026-08-05 | `8877172` | 增加 Candidate BFF HTTP 回归检查 |
| 2026-08-05 | `b3a3553` | 增加 Candidate Playwright Mock 回归 |
| 2026-08-05 | `13d6116` | 扩展考生系统回归覆盖 |
| 2026-08-05 | `a685105` | 补充候选人业务 Handler 测试 |
| 2026-08-05 | `1a5a399` | 增加真实测试环境只读回归任务 |
| 2026-08-06 | `677cca0` | 补齐 Mock 业务流程、BFF 写接口测试和真实环境回归可写阶段 |
| 2026-08-06 | 本次提交 | 将真实环境只读和可写阶段合并到任务三 |
| 2026-08-06 | 本次提交 | 增加认证购买、Stripe Test Mode 支付、课程学习和测验完整旅程 |
